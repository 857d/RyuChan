---
const { slug, encryptedContent, password } = Astro.props;
---

<div style="border: 4px solid red; padding: 20px; margin: 20px; background: #f0f0f0;">
  <h3>🔐 加密组件测试</h3>
  <p>slug: {slug}</p>
  <p>password: {password}</p>
  <p>encryptedContent 长度: {encryptedContent?.length || 0}</p>
</div>
